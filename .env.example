# Session Secret (generate a random string for production)
SESSION_SECRET=your-secret-key-here

# AI API Key (optional - for content generation)
GEMINI_API_KEY=your-gemini-api-key-here

# Telegram Bot (optional - configure via Settings page after deployment)
# These are stored in app settings, not environment variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Note: Application uses in-memory storage by default
# For persistent storage, database integration can be added later
